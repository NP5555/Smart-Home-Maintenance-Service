{
  "name": "@smart-home/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./client": "./dist/client.js",
    "./prisma": "./prisma"
  },
  "scripts": {
    "db:migrate": "node scripts/dbmate.mjs up",
    "db:new": "node scripts/dbmate.mjs new",
    "db:status": "node scripts/dbmate.mjs status",
    "db:pull": "dotenv -e ../../.env -- prisma db pull --schema prisma/schema.prisma && npm run db:generate",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:reset": "npm run db:drop && npm run db:migrate && npm run db:pull && npm run db:seed",
    "db:drop": "node scripts/dbmate.mjs drop --force",
    "db:seed": "dotenv -e ../../.env -- tsx seed/seed.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src seed",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prisma/client": "6.12.0",
    "argon2": "^0.43.0"
  },
  "devDependencies": {
    "@smart-home/config": "*",
    "@types/node": "^22.16.4",
    "dbmate": "^2.36.0",
    "dotenv-cli": "^8.0.0",
    "prisma": "6.12.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
