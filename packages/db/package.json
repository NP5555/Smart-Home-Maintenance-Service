{
  "name": "@smart-home/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./client": "./dist/client.js",
    "./prisma": "./prisma"
  },
  "scripts": {
    "db:migrate": "dbmate --migrations-dir migrations --no-dump-schema up",
    "db:new": "dbmate --migrations-dir migrations --no-dump-schema new",
    "db:status": "dbmate --migrations-dir migrations --no-dump-schema status",
    "db:pull": "prisma db pull --schema prisma/schema.prisma && prisma generate --schema prisma/schema.prisma",
    "db:generate": "prisma generate --schema prisma/schema.prisma",
    "db:reset": "npm run db:drop && npm run db:migrate && npm run db:pull && npm run db:seed",
    "db:drop": "dbmate --migrations-dir migrations --no-dump-schema drop --force",
    "db:seed": "tsx seed/seed.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src seed",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prisma/client": "6.12.0"
  },
  "devDependencies": {
    "@smart-home/config": "*",
    "@types/node": "^22.16.4",
    "dbmate": "^2.36.0",
    "prisma": "6.12.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
